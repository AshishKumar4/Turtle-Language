a = 10;

define test(a,b) 
{ 
    a * 4 + b;
};

define new(a,b) 
{
    o = test(a*a, b)*test(4, a+b); 
    test(o,o); 
};

wow = lambda (x) { x("THIS WORKS!") };

sum = 0;

i = 1000;

while (i)
{
    sum = sum + i*2 + 5;
    i = i - 1;
};

o = 0;
if(0)
{
    o = 1;
}
elif (1)
{
    o = 2;
}
else
{
    o = 3;
};

x = test(4,5);
y = new(4,5);
z = new(2,3);

u = test(new(4,5), new(2,3));
v = test(y, z);

a = [5,2,7,[8,10],1,4];

(x,y,z, sum, o, wow(lambda (x){ "WOW " + x } ), u, v, a + 5, a[2,3]);